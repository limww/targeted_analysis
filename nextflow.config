profiles {
    slurm {
        process.executor = 'slurm'
		process.memory = '20 GB'
		process.cpus = 20    
    }
}
manifest.defaultBranch = 'main'

singularity {
        enabled = true
        cacheDir = '/prism_data5/share/wanwan/Singularity_cache/' 
	    autoMounts = true
}
